# Canonical current truth. Keep exactly one active slice; history belongs in Git.
version: "projectstate-template-v6"
profile: core
project:
  outcome_ref: "PROJECT.md#outcome"
current_slice:
  id: alpha15-public-install-001
  objective: "Publish one accurate StatePort 0.1.0-alpha.15 site whose Alpha.15 one-line installer delivers the complete product on fresh Windows 11 WSL2 Ubuntu 24.04, then enlarge the public header mascot to about 1.75 times its current visual size."
  status: implementing
  # Human-owned. Agents may report results but may not weaken these criteria.
  acceptance:
    - "One consistent public documentation path gives a fresh Windows 11 WSL2 Ubuntu 24.04 AMD64 user exactly one anonymous installer command, then clear open-product, template, troubleshooting, update, and uninstall guidance."
    - "The signed index, versioned URL, displayed version, source identity, image manifests, mutable installer, and installed receipt all identify Alpha.15 exactly."
    - "The additive Alpha.15 tree is deployed and anonymously byte-verified while every anchored earlier versioned release tree remains unchanged."
    - "The public command installs the web UI, control API, worker, execution host, persistence, restart and reboot behavior, and supports the actual ProjectState and StudyState repositories plus an independent third valid template."
    - "The site remains accessible without JavaScript and contains no analytics, tracking, third-party runtime scripts, secrets, or mutable artifact references."
  primary_journey:
    description: "From a genuinely fresh Windows 11 WSL2 Ubuntu 24.04 AMD64 environment, follow the public docs, run the anonymous command, exercise the complete product and three template types, reboot, uninstall, and reinstall."
    command: "bash <(curl -fsSL https://lennertvhoy.github.io/StatePort-Site/download/install.sh)"
    environment: "Fresh Windows 11 AMD64 host; WSL2; stock Ubuntu 24.04; normal sudo-capable user; anonymous public Pages and GHCR; no checkout, prepared packages, mirrors, shims, or staged files"
    status: staged_simulation_passed_native_pending
    evidence: "evidence/alpha15-public-install-001/summary.md"
validation:
  primary_journey: staged_simulation_passed_native_pending
  automated_tests: in_progress
  human_acceptance: pending
delivery_boundary:
  name: anonymous_fresh_wsl2_complete_product
  failed_attempts: []
  simplification_review: null
blockers: []
risks:
  - id: alpha15-site-not-yet-published
    category: delivery
    severity: high
    exposure: local_only
    affected_environment: "StatePort-Site working tree"
    consequence: "The signed Alpha.15 artifacts and updated documentation exist locally but the public Pages route still serves the predecessor until validation, commit, push, deployment, and anonymous verification finish."
    owner: agent
    decision: mitigate
    expires: null
next_action: "Finish the Site v6 validator migration and docs QA, increase the public header mascot to about 1.75x without breaking mobile layout, commit and deploy Alpha.15, anonymously verify Pages bytes and the public one-line path, then run the genuine fresh Windows WSL2 product journey."
